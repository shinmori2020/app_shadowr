# box-shadowジェネレーター「Shadowr」企画書

## 1. アプリ概要

| 項目 | 内容 |
| --- | --- |
| アプリ名 | Shadowr |
| 目的 | Web制作時にbox-shadowを視覚的に調整し、CSSコードを即座に取得する |
| 対象ユーザー | 自分（Web制作者） |
| 技術 | React + Vite + Tailwind CSS |
| 外部API | 不要（すべてフロントエンドで完結） |
| デザイン | シンプル・ミニマル（Swatchr / Gradientrと統一） |

---

## 2. 機能一覧

### 2-1. シャドウパラメータ調整

スライダーで以下のパラメータをリアルタイムに調整できる。

| パラメータ | CSS属性 | 範囲 | 初期値 |
| --- | --- | --- | --- |
| X軸オフセット | offset-x | -100px 〜 100px | 5px |
| Y軸オフセット | offset-y | -100px 〜 100px | 5px |
| ぼかし | blur-radius | 0px 〜 100px | 15px |
| 広がり | spread-radius | -50px 〜 50px | 0px |
| 色 | color | カラーピッカー | #00000033 |
| 不透明度 | opacity（色に含む） | 0% 〜 100% | 20% |
| 内側シャドウ | inset | ON / OFF | OFF |

### 2-2. 複数シャドウ対応

- 最大4つまでシャドウレイヤーを追加可能
- 各レイヤーを個別に調整
- レイヤーのON/OFF切り替え（一時的に無効化）
- レイヤーの削除

### 2-3. リアルタイムプレビュー

- 調整のたびにプレビューボックスのシャドウが即座に更新
- プレビューボックスの背景色を変更可能（白 / ダーク / カスタム）
- プレビューボックスの形状切り替え（正方形 / 角丸 / 円）

### 2-4. CSSコード出力・コピー

- 生成されたCSSコードを表示
- ワンクリックでコピー
- 出力形式: `box-shadow: ...;`

### 2-5. プリセット

- よく使うシャドウパターンをプリセットとして用意
- クリックで即座に適用

---

## 3. 画面構成

### メイン画面（1画面構成）

```
┌──────────────────────────────────────────────────┐
│  Shadowr                                         │
├──────────────────────────────────────────────────┤
│                                                  │
│  ┌──────────────────────────────────────────┐    │
│  │                                          │    │
│  │         ┌──────────────┐                 │    │
│  │         │              │                 │    │
│  │         │  プレビュー    │  ← box-shadow  │    │
│  │         │    ボックス    │    適用         │    │
│  │         │              │                 │    │
│  │         └──────────────┘                 │    │
│  │                                          │    │
│  └──────────────────────────────────────────┘    │
│                                                  │
│  ▼ プレビュー設定                                  │
│  背景: [ ライト | ダーク | カスタム ]               │
│  形状: [ □ ] [ ▢ ] [ ○ ]                         │
│                                                  │
│  ▼ レイヤー 1                          [ON] [×]  │
│  ┌─────────────────────────────────────┐         │
│  │ X:     [────●────────] 5px          │         │
│  │ Y:     [────●────────] 5px          │         │
│  │ ぼかし: [──────●──────] 15px         │         │
│  │ 広がり: [────────●────] 0px          │         │
│  │ 色:    🎨 #000000  不透明度: [──●──] 20% │     │
│  │ □ inset                              │         │
│  └─────────────────────────────────────┘         │
│                                                  │
│  [ + レイヤーを追加 ]                              │
│                                                  │
│  ▼ CSSコード                                      │
│  ┌─────────────────────────────────────┐         │
│  │ box-shadow: 5px 5px 15px 0px        │ [Copy]  │
│  │   rgba(0, 0, 0, 0.2);              │         │
│  └─────────────────────────────────────┘         │
│                                                  │
│  ▼ プリセット                                     │
│  [ Subtle ] [ Medium ] [ Bold ] [ Float ] ...    │
│                                                  │
└──────────────────────────────────────────────────┘
```

---

## 4. 技術設計

### 4-1. プロジェクト構成

```
shadowr/
├── public/
├── src/
│   ├── components/
│   │   ├── layout/
│   │   │   └── AppLayout.jsx           # レイアウト（共通）
│   │   ├── ui/
│   │   │   ├── CopyButton.jsx          # コピーボタン（共通）
│   │   │   ├── CodeBlock.jsx           # コード表示（共通）
│   │   │   ├── Toast.jsx               # 通知（共通）
│   │   │   └── Slider.jsx              # ラベル付きスライダー（新規）
│   │   └── features/
│   │       ├── ShadowPreview.jsx        # プレビュー表示
│   │       ├── PreviewSettings.jsx      # プレビュー背景・形状切り替え
│   │       ├── ShadowLayerList.jsx      # レイヤー一覧
│   │       ├── ShadowLayerItem.jsx      # 個別レイヤー設定
│   │       └── PresetList.jsx           # プリセット一覧
│   ├── hooks/
│   │   └── useClipboard.js             # クリップボード（共通）
│   ├── utils/
│   │   └── shadowGenerator.js           # box-shadow CSS文字列生成
│   ├── constants/
│   │   └── presets.js                   # シャドウプリセット定義
│   ├── App.jsx
│   ├── index.css
│   └── main.jsx
├── docs/
│   └── plan.md
├── .cursorrules
├── package.json
├── vite.config.js
└── README.md
```

### 4-2. 状態管理（App.jsx）

```jsx
const [layers, setLayers] = useState([
  {
    id: 1,
    enabled: true,
    offsetX: 5,
    offsetY: 5,
    blur: 15,
    spread: 0,
    color: '#000000',
    opacity: 20,
    inset: false,
  },
]);

const [previewBg, setPreviewBg] = useState('light');    // 'light' | 'dark' | 'custom'
const [customBg, setCustomBg] = useState('#f0f0f0');
const [previewShape, setPreviewShape] = useState('rounded'); // 'square' | 'rounded' | 'circle'
```

### 4-3. CSS生成ロジック（shadowGenerator.js）

```jsx
/**
 * シャドウレイヤー配列からbox-shadow CSS文字列を生成する
 * @param {Array} layers - シャドウレイヤー配列
 * @returns {string} box-shadow CSS文字列
 */
export const generateShadowCSS = (layers) => {
  const enabledLayers = layers.filter(layer => layer.enabled);

  if (enabledLayers.length === 0) return 'none';

  return enabledLayers
    .map(layer => {
      const rgba = hexToRgba(layer.color, layer.opacity);
      const inset = layer.inset ? 'inset ' : '';
      return `${inset}${layer.offsetX}px ${layer.offsetY}px ${layer.blur}px ${layer.spread}px ${rgba}`;
    })
    .join(', ');
};

/**
 * HEXカラーと不透明度からrgba文字列を生成する
 * @param {string} hex - HEXカラーコード
 * @param {number} opacity - 不透明度（0〜100）
 * @returns {string} rgba文字列
 */
export const hexToRgba = (hex, opacity) => {
  const r = parseInt(hex.slice(1, 3), 16);
  const g = parseInt(hex.slice(3, 5), 16);
  const b = parseInt(hex.slice(5, 7), 16);
  const a = (opacity / 100).toFixed(2);
  return `rgba(${r}, ${g}, ${b}, ${a})`;
};
```

### 4-4. プリセット定義（presets.js）

```jsx
export const SHADOW_PRESETS = [
  {
    name: 'Subtle',
    layers: [
      { offsetX: 0, offsetY: 1, blur: 3, spread: 0, color: '#000000', opacity: 12, inset: false },
    ],
  },
  {
    name: 'Medium',
    layers: [
      { offsetX: 0, offsetY: 4, blur: 6, spread: -1, color: '#000000', opacity: 10, inset: false },
      { offsetX: 0, offsetY: 2, blur: 4, spread: -2, color: '#000000', opacity: 10, inset: false },
    ],
  },
  {
    name: 'Bold',
    layers: [
      { offsetX: 0, offsetY: 10, blur: 15, spread: -3, color: '#000000', opacity: 15, inset: false },
      { offsetX: 0, offsetY: 4, blur: 6, spread: -4, color: '#000000', opacity: 10, inset: false },
    ],
  },
  {
    name: 'Float',
    layers: [
      { offsetX: 0, offsetY: 20, blur: 25, spread: -5, color: '#000000', opacity: 15, inset: false },
      { offsetX: 0, offsetY: 8, blur: 10, spread: -6, color: '#000000', opacity: 10, inset: false },
    ],
  },
  {
    name: 'Inset',
    layers: [
      { offsetX: 0, offsetY: 2, blur: 4, spread: 0, color: '#000000', opacity: 15, inset: true },
    ],
  },
  {
    name: 'Sharp',
    layers: [
      { offsetX: 5, offsetY: 5, blur: 0, spread: 0, color: '#000000', opacity: 20, inset: false },
    ],
  },
];
```

---

## 5. 共通コンポーネント（流用リスト）

| コンポーネント/ファイル | 流用元 |
| --- | --- |
| AppLayout.jsx | Swatchr / Gradientr |
| CopyButton.jsx | Swatchr / Gradientr |
| CodeBlock.jsx | Swatchr / Gradientr |
| Toast.jsx | Swatchr / Gradientr |
| useClipboard.js | Swatchr / Gradientr |
| .cursorrules（ベース部分） | Swatchr / Gradientr |
| index.css | Swatchr / Gradientr |

### 新規コンポーネント

| コンポーネント | 説明 |
| --- | --- |
| Slider.jsx | ラベル・値表示付きの汎用スライダー（複数パラメータで繰り返し使用） |

---

## 6. 開発ステップ

### Step 1: プロジェクトセットアップ

- Vite + React + Tailwind CSS の環境構築
- 共通コンポーネントをコピー

### Step 2: 定数・ユーティリティ

- プリセット定義
- box-shadow CSS生成関数（generateShadowCSS, hexToRgba）

### Step 3: 汎用コンポーネント

- Slider コンポーネント（ラベル + スライダー + 値表示）

### Step 4: 機能コンポーネント

- ShadowPreview（プレビューボックス）
- PreviewSettings（背景色・形状切り替え）
- ShadowLayerItem（個別レイヤーのパラメータ調整）
- ShadowLayerList（レイヤー一覧、追加・削除）
- PresetList（プリセット一覧）

### Step 5: 統合・調整

- 全体レイアウトの調整
- レスポンシブ対応
- 動作確認・デザイン微調整

---

## 7. 将来の拡張案（v2以降）

- レイヤーのドラッグ並べ替え
- text-shadow 対応
- シャドウの保存・履歴機能（localStorage）
- Tailwind CSS クラス名での出力（shadow-lg 等との比較表示）
- アニメーション付きシャドウのプレビュー（hover時の変化）
